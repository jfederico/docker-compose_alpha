# Optional. Prefer one Cloudflare tunnel with public hostnames managed in the
# Cloudflare dashboard, instead of one tunnel container per domain.
CLOUDFLARE_TUNNEL_TOKEN=replace-with-cloudflare-tunnel-token
