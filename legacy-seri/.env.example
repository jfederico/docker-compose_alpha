# Bind the temporary bridge on Seri.
#
# Use 127.0.0.1 only for local testing. For alpha-to-Seri traffic, set this to
# Seri's LAN IP and firewall the port so only alpha can reach it.
ALPHA_BRIDGE_BIND=127.0.0.1
